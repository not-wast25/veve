import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: "'Lexend', sans-serif",  // Hier die gewünschte Schriftart angeben
    fontWeightLight: 100, 
  },
});

export default theme;
